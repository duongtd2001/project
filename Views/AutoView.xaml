<UserControl x:Class="project.Views.AutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:project.Views"
             xmlns:viewModel="clr-namespace:project.ViewModels"
             xmlns:cal="http://www.caliburnproject.org"
             d:DesignHeight="800" d:DesignWidth="785">
    <UserControl.Resources>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                      From="0" To="1" Duration="0:0:0.25" FillBehavior="HoldEnd"/>
        </Storyboard>
    </UserControl.Resources>
    <Border Grid.Column="0" BorderThickness="2,2,1,2" Background="#200F53" CornerRadius="15">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>
            <Border  BorderThickness="0,0,0,2">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource winBoderColor1}" Offset="0"/>
                        <GradientStop Color="{StaticResource winBoderColor2}" Offset="0.75"/>
                        <GradientStop Color="{StaticResource winBoderColor3}" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <StackPanel Grid.Row="0">
                    <Label Content="AUTO" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                </StackPanel>
            </Border>
            <Border Grid.Row="2" BorderThickness="1">

                <StackPanel Orientation="Horizontal">
                    <RadioButton Style="{StaticResource menuButton2}"
                             Tag="{StaticResource color1}"
                             IsChecked="True"
                             Command="{Binding ShowResultsLog}">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="RESULTS" Style="{StaticResource menuButtonText}"
                                   Margin="5"/>
                        </StackPanel>

                    </RadioButton>
                </StackPanel>

            </Border>

            <Border Grid.Row="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="70"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="150"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="150"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="80"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2">
                                <UniformGrid Columns="2" HorizontalAlignment="Stretch">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="POSITION (mm)" Style="{StaticResource menuButtonText2}"/>
                                    </StackPanel>
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="SPEED (mm/s)" Style="{StaticResource menuButtonText2}"/>
                                    </StackPanel>
                                </UniformGrid>
                            </Border>
                            <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2">
                                <UniformGrid Columns="2" HorizontalAlignment="Stretch">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBox Text="{Binding mPosServo, Mode=OneWay}" Height="35" Width="200" Style="{StaticResource CustomTextBoxStyle}" IsEnabled="False" HorizontalContentAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBox Text="{Binding mSpeedServo, Mode=OneWay}" Height="35" Width="200" Style="{StaticResource CustomTextBoxStyle}" IsEnabled="False" HorizontalContentAlignment="Center"/>
                                    </StackPanel>
                                </UniformGrid>
                            </Border>
                        </Grid>
                    </Border>
                    <Border Grid.Row="100">
                        <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft">
                            <Button x:Name="INSERTBALL_Auto" Background="{Binding aIsINSERTBALL}"
                                Height="80" Width="200" Style="{StaticResource GradientBorderButton}">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="INSERT BALL" Style="{StaticResource menuButtonText2}" FontSize="20"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="AirSPCommand"
                                    Background="{Binding aISAirSP}"
                                    Height="80" Width="200"
                                    Margin="10,0,10,0" Style="{StaticResource GradientBorderButton}">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="AirSP" Style="{StaticResource menuButtonText2}" FontSize="20"/>
                                </StackPanel>
                            </Button>

                        </StackPanel>
                    </Border>
                    <Border Grid.Row="2">
                        <UniformGrid Columns="5" HorizontalAlignment="Stretch">
                            <Button x:Name="SelectPos1_Auto" Background="{Binding aIsBall1}" Margin="20,0,20,0" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 1" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                            <Button x:Name="SelectPos2_Auto" Background="{Binding aIsBall2}" Margin="20,0,20,0" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 2" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                            <Button x:Name="SelectPos3_Auto" Background="{Binding aIsBall3}" Margin="20,0,20,0" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 3" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                            <Button x:Name="SelectPos4_Auto" Background="{Binding aIsBall4}" Margin="20,0,20,0" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 4" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                            <Button x:Name="SelectPos5_Auto" Background="{Binding aIsBall5}" Margin="20,0,20,0" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 5" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                        </UniformGrid>
                    </Border>
                    <Border Grid.Row="4">
                        <UniformGrid Columns="5" HorizontalAlignment="Stretch">
                            <Button x:Name="SelectPos6_Auto" Margin="20,0,20,0" Background="{Binding aIsBall6}" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 6" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                            <Button x:Name="SelectPos7_Auto" Margin="20,0,20,0" Background="{Binding aIsBall7}" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 7" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                            <Button x:Name="SelectPos8_Auto" Margin="20,0,20,0" Background="{Binding aIsBall8}" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 8" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                            <Button x:Name="SelectPos9_Auto" Margin="20,0,20,0" Background="{Binding aIsBall9}" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 9" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                            <Button x:Name="SelectPos10_Auto" Margin="20,0,20,0" Background="{Binding aIsBall10}" Style="{StaticResource GradientBorderButton}">
                                <TextBlock Text="Ball 10" Style="{StaticResource menuButtonText2}" FontSize="30"/>
                            </Button>
                        </UniformGrid>
                    </Border>
                </Grid>
            </Border>
            <Border Grid.Row="3">
                <StackPanel>
                    <TextBox Height="35" Text="{Binding aResults}"
                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto" 
                         Style="{StaticResource CustomTextBoxStyle}"
                         IsReadOnly="True"/>
                </StackPanel>
            </Border>
        </Grid>

    </Border>
</UserControl>
